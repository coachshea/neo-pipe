#include <stdlib.h>
#include <stdio.h>

#define CTRLD   4

/**
 * to store an int (id) into void*
 *
 * void *ptr = malloc(sizeof(int));
 * *((int*)ptr) = 5;
 * printf("%d\n",*((int*)ptr));
 */

int
main()
{
  printf("helloo %s", "john");
}
